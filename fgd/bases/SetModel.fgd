// Entities with a settable model.
@BaseClass base(SetSkin) 
	studioprop()
= SetModel
	[
		linedivider_prop[!engine](string) readonly : "----------------------------------------------------------------------------------------------------------" : "" : "Prop"
	model(studio) : "World Model" : : "The model to use for this entity."
	]
